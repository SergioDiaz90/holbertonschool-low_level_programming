#include "holberton.h"
/**
* main - Print the alphabet in lowercase of the 'a' to the 'z'.
*
* Return: Always 0.
*/
void print_alphabet(void)
{
	int x;

	for(x = 'a'; x <= 'z' ; x++)
	{
		_putchar(x);
	}
	_putchar('\n');
}
